import Foundation

struct AppConfig {
    // Replace this with your actual Railway Public Domain (e.g. "https://poker-app-production.up.railway.app")
    // Note: Make sure it starts with "https://" and does NOT have a trailing slash
    static let serverURL = "https://poker-app-production-d3a8.up.railway.app"
}
